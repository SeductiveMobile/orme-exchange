# Python app external packages

* flask
* ipython
* web3
* python-bitcoinrpc
* requests


## Uninstall all packages

`pip freeze | xargs pip uninstall -y`

## Install all required packages

`pip install ipython flask requests web3 python-bitcoinrpc`
`pip freeze > requirements.txt`
